# Adding a new service

Subsitute "NewService" for the name of the service being added.

## Create a new runner in mediagarrd/services/runner/ named NewServiceRunner.java

## Create a new ServiceConfig if needed, otherwise use the CommonServiceConfig
## Create a new ServiceConfig if needed, otherwise use the CommonServiceConfig
package wellatleastitried.mediagarrd.services.config;

public class NewServiceServiceConfig extends CommonServiceConfig {}
